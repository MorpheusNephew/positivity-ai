[tool.poetry]
name = "backend"
version = "0.1.0"
description = "Backend for Positivity.ai"
authors = ["Jelani Jackson"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
